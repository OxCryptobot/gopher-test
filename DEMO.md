# GOPHER AI — demo prep (item 97)

Honest status: **stills exist. Video not yet.** Do not mark SHIP item 97 done until a real reel ships.

Public face: **GOPHER AI**. Price: **$19/month**. Waitlist is the door. Capture production (or a clean local hole), never the TEST ribbon as the product.

## Existing stills (`shots/`)

| File | What |
| --- | --- |
| `app-home.png` | Directory + prompt (narrow PWA shot) |
| `app-fetch.png` | FETCH / MAZE panel |
| `hero.png` | Hero / wordmark still |
| `ai-hero.png` | Alternate hero |
| `ai-live.png` | Live prompt still |
| `mid.png` | Mid-scroll / mid-flow |
| `footer.png` | Footer chrome |
| `play-before.png` / `play-mid.png` / `play-after.png` | MAZE play sequence |
| `play2-start.png` / `play2-mid.png` / `play2-after.png` | Second play pass |

Press copies live under `press/` (`home.png`, `fetch.png`, `icon.png`, plus staging refs). Share card: `press/share-card.svg`.

## How to capture FETCH + directory (for the reel)

1. Open production https://oxcryptobot.github.io/gopher/ (or local `python3 server.py`). Hard-refresh after a cache bump.
2. **Directory:** home `#/` — numbered selectors, GOPHER prompt, phosphor on `#070908`. Shot full phone frame if possible.
3. **FETCH search:** type `fetch btc` (or ETH) in the prompt — search result, not the maze. Say “FETCH is search” in the voiceover.
4. **MAZE side quest (optional B-roll):** `#/fetch` → START — one tap, one tile. Label it a side quest, not the product.
5. **DIG / tasks:** type `tasks` or open Tasks/ — one live skill, note parked stay parked.
6. **Waitlist door:** `#/pricing` then `#/waitlist` — say **$19/month**, checkout parked.
7. Export stills to `shots/` (PNG). Keep phosphor; no stock footage; no invented metrics.

## Video not yet

- Hole `#/demo` points at these stills and says video is not shipped.
- When a short reel exists, drop it next to `shots/`, link from `/demo` + `press/`, then mark item **97** in `SHIP.md`.

Type `demo` on the hole. Progress stays **83/100** until something truly ships.
